// Документ "Поступление товаров" — модуль объекта.
// Движения: РегистрНакопления.ОстаткиМатериалов (Приход).
// Регистр ОстаткиМатериалов нужно создать (см. README.md), в документе отметить его на закладке "Движения".

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)

	Для Каждого Строка Из Материалы Цикл
		Строка.Сумма = Строка.Количество * Строка.Цена;
	КонецЦикла;

КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, Режим)

	Движения.ОстаткиМатериалов.Записывать = Истина;

	Для Каждого Строка Из Материалы Цикл
		Движение = Движения.ОстаткиМатериалов.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Приход;
		Движение.Период      = Дата;
		Движение.Материал    = Строка.Материал;
		Движение.Количество  = Строка.Количество;
		Движение.Сумма       = Строка.Количество * Строка.Цена;
	КонецЦикла;

КонецПроцедуры
