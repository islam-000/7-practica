// Документ "Заказ на мойку" — модуль объекта.
// Документ ничего не проводит: выручка фиксируется документом "Оплата".
// Здесь: пересчёт сумм и проверка перед записью.

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)

	Для Каждого Строка Из Услуги Цикл
		Строка.Сумма = Строка.Количество * Строка.Цена;
	КонецЦикла;
	Сумма = Услуги.Итог("Сумма");

КонецПроцедуры

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)

	Если Не ЗначениеЗаполнено(Статус) Тогда
		Статус = Перечисления.СтатусЗаказа.Новый;
	КонецЕсли;

КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)

	Если Услуги.Количество() = 0 Тогда
		Сообщить("Добавьте хотя бы одну услугу.");
		Отказ = Истина;
	КонецЕсли;

КонецПроцедуры
